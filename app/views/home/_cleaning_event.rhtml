<div id="cleaning-event-<%=story.id%>" class="cleaning-event">				
	<%
	@story = story
	%>
	<div class="event-image">
		<%if @story.pictures[0] == nil%>
			No Picture Provided
		<% else %>
			<img src="<%=url_for(:controller => "home" , :action => :picture, :id => @story.pictures[0])%>" height="230"/>
		<% end %>
	</div>
		
	<div class="event-map">	
		<% if @story.geo_location == nil%>
			No Location Provided
		<%else%>
			<%
			map = GMap.new("map_div_#{@story.id}")
		    map.control_init(:small_map => true,:map_type => true)							    
		    map.set_map_type_init(GMapType::G_HYBRID_MAP)      							
		    geo_location  = @story.geo_location
		    map.center_zoom_init([geo_location.lat,geo_location.lng],8)  	  						
			marker = GMarker.new([geo_location.lat, geo_location.lng], :title => geo_location.name)  
			map.overlay_init marker							
			%>
			<%= map.to_html %>
			<%= map.div(:width => 305, :height => 230) %>
		<%end%>
	</div>
	
	<div class="event-story">
		<table width=100%>
			<tr>
				<td width=60%>
					<div class="event-location">
						<%=@story.geo_location.name%>
					</div>
				</td>
				<td width=40% align="right">
					<div class="event-date">
						<img src="/images/new/calendar_icon.png"/>
						<%=@story.cleaning_at.to_s(:pretty)%>						
					</div>
				</td>
			</tr>
			<tr>
				<td width=60%>
					<div class="event-weight">
						<b>Weight:</b>&nbsp;<%= pluralize @story.weight, 'pound' -%>.
					</div>
				</td>
				<td width=40% align="right">
					<div class="event-user">
						<%= @story.user.login %>
					</div>
				</td>
			</tr>
		</table>
		<div class="event-blog">
			<%= @story.blog%>
		</div>
	</div>
</div>
